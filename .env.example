DATABASE_URL=postgresql+psycopg://antho@localhost:5432/veridicta_m1
TEST_DATABASE_URL=postgresql+psycopg://antho@localhost:5432/veridicta_test
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
REDIS_URL=redis://localhost:6379/0
AUTH_SECRET=replace-with-a-long-random-local-secret
# Keep disabled outside deliberate local account-seeding work.
AUTH_ALLOW_CREDENTIAL_SIGNUP=false
AUTH_DEMO_LOGIN_NAME="Anthony Logan"
AUTH_DEMO_LOGIN_EMAIL="anthony.logan@veridicta.local"
# Set to "true" for local previews to skip auth and route "Send magic link" + "Explore Platform"
# directly into the dashboard with a demo user. Do NOT enable in production.
AUTH_DEV_BYPASS=false
AUTH_EMAIL_FROM="Veridicta <auth@example.com>"
RESEND_API_KEY=
BETA_LEAD_TO=hello@veridicta.nl
# Local sandbox sender; production must use a Resend-verified company-domain sender.
BETA_LEAD_FROM="Veridicta <onboarding@resend.dev>"
OPENAI_API_KEY=
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
OPENAI_CHAT_MODEL=gpt-4.1
